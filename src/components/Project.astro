---
import { getProjects } from "../lib/project";
import ShinyText from "./animations/ShinyText/ShinyText";
---

<!-- 
    title: string;
    description: string;
    image: string;
    link: string;
    tags: {
        name: string;
        color: string;
        textColor: string;
    }[];
-->

<div class="grid grid-cols-1 md:grid-cols-2 gap-8">
    {getProjects().map((project) => (
        <div class="bg-gray-50 rounded-lg p-6 hover:shadow-lg transition-shadow">
            <h3 class="text-xl font-semibold mb-3">{project.title}</h3>
            <p class="text-gray-600 mb-4">{project.description}</p>
            <div class="flex flex-wrap gap-2 mb-4">
                {project.tags.map((tag) => (
                    <span 
                        class=`px-3 py-1 rounded-full text-sm`
                        style={`background-color: ${tag.color}; color: ${tag.textColor};`}
                    >
                        {tag.name}
                    </span>
                ))}
            </div>
            {project.link && (
                <a href={project.link} target="_blank" class="border-2 border-green-100 rounded-md px-4 py-2 bg-green-100">
                    <ShinyText
                        client:load
                    text="View Project"
                    disabled={false}
                    speed={5}
                        className="bg-green-500"
                    />
                </a>
            )}
        </div>
    ))}
</div>